import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles(() => ({
  initContainer: {
    paddingTop: 20,
    paddingBottom: '40vh'
  },
  grid: {
    margin: 'auto',
    marginTop: 20,
  },
}));

export default styles;
