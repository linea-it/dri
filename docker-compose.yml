version: '2'
services:
  dri:
    user: 'dri'
    build: .
    volumes:
      - ./:/app
      - ./dri.conf:/etc/apache2/sites-enabled/dri.conf
    command: service apache2 restart
    image: dri
    ports:
     - "80:80"
    environment:
     - "VIRTUAL_HOST=dri.com"
    tty: true
